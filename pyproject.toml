[project]
name = "ychance"
version = "0.0.0"
description = "Discovery-loop methodology runtime"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "hypothesis>=6.100",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
