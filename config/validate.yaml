# Validate stage protocol parameters. Revisable at M2a only.

validate:
  config_version: validate-v1
  max_rule_compute_usd: 0.05
  compute_cost_per_bar_usd: 0.000001
  compute_cost_per_trade_usd: 0.000001
  data_read_cost_usd: 0.000001
  outer_folds: 3
  holdout_bars: 20
  min_train_bars: 40
  inner_folds: 2
  min_gap_bars: 1
  max_dependency_gap_bars: 10000
  round_trip_cost_bps: 10.0
  tax_rate: 0.0
  drawdown_penalty: 0.0
  min_return_floor: 0.000001
  dominance_order: 1
  dominance_epsilon: 0.0
  min_challenger_pass_fraction: 1.0
  require_strict_dominance: false
  context_random_seed: 1
  context_shift_fraction: 0.05
